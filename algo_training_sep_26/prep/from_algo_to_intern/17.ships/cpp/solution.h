#include <algorithm>
#include <cmath>
#include <iostream>
#include <map>
#include <numeric>
#include <ranges>
#include <string>
#include <vector>

class Solution {
 public:
  std::string getSolution(std::vector<std::string> map) {
    for (auto s : map) {
      std::cout << s << std::endl;
    }

    std::string res = {"YES"};
    return res;
  }
};
